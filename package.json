{
  "name": "zedongjia.github.io",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "LAST_UPDATED=$(git log -1 --format=%cs) vite",
    "build": "LAST_UPDATED=$(git log -1 --format=%cs) vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "js-yaml": "^4.2.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "vite": "^6.3.5"
  }
}
